import React from "react";

const Filters = () => {
  return (
    <div
      style={{
        fontSize: "40px",
        color: "red",
      }}>
      LEO MESSI
    </div>
  );
};

export default Filters;
